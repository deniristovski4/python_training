# Write a Python program that accepts an numbers of days (integer ) and computes the total number of seconds for the specified days value.

days=input("Внеси број на денови: ")

seconds=int(days)*24*60*60

print(seconds)